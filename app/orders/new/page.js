import Shell from '@/components/Shell';import { requireAuth } from '@/lib/guard';
export default async function NewOrder(){await requireAuth();return <Shell><div className="top"><div><div className="eyebrow">Manual / test entry</div><h1>New order</h1><div className="muted">Same normalized record Etsy imports will create.</div></div></div><div className="card" style={{maxWidth:760}}><form className="form" action="/api/orders" method="post"><div className="row"><div className="field"><label>Child name</label><input name="child_name" required/></div><div className="field"><label>Character</label><select name="character_label"><option>Curly Blonde</option></select></div></div><div className="field"><label>Story</label><select name="product_slug"><option value="hidden-door">The Hidden Door</option></select></div><div className="field"><label>Dedication</label><textarea name="dedication" placeholder="Love always, Grandma & Grandpa"/></div><div className="row"><div className="field"><label>Buyer name</label><input name="buyer_name"/></div><div className="field"><label>Quantity</label><input name="quantity" type="number" min="1" defaultValue="1"/></div></div><button className="btn">Create order</button></form></div></Shell>}
