export default async function Login({searchParams}){const p=await searchParams;return <div className="login"><div className="card"><div className="eyebrow">Private workspace</div><h1>AMI Etsy Ops</h1><p className="muted">Enter the internal operations password.</p>{p?.error&&<p className="notice warn">Incorrect password.</p>}<form className="form" action="/api/auth/login" method="post"><div className="field"><label>Password</label><input type="password" name="password" autoFocus required/></div><button className="btn">Enter workspace</button></form></div></div>}
